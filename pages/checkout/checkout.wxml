<view class="checkout-page">
  <view class="section">
    <view class="section-title">选择桌号</view>
    <view class="table-list">
      <view
        class="table-item {{tableIndex === index ? 'active' : ''}}"
        wx:for="{{tables}}"
        wx:key="id"
        wx:for-index="index"
        bindtap="selectTable"
        data-index="{{index}}"
      >
        <text class="table-name">{{item.name}}</text>
        <text class="table-area">{{item.area}}</text>
      </view>
    </view>
  </view>

  <view class="section">
    <view class="section-title">菜品清单</view>
    <view class="item-list">
      <view class="item" wx:for="{{items}}" wx:key="specKey">
        <view class="item-left">
          <view class="item-name">{{item.name}}</view>
          <view class="item-specs" wx:if="{{item.specs && item.specs.length > 0}}">
            <text wx:for="{{item.specs}}" wx:key="name" wx:for-item="spec">
              {{spec.name}}：{{spec.value}} 
            </text>
          </view>
        </view>
        <view class="item-right">
          <text class="item-qty">x{{item.quantity}}</text>
          <text class="item-price">¥{{item.subtotal}}</text>
        </view>
      </view>
    </view>
  </view>

  <view class="section">
    <view class="section-title">订单备注</view>
    <textarea
      class="remark-input"
      placeholder="口味、忌口、打包等要求"
      value="{{remark}}"
      bindinput="onRemarkInput"
      maxlength="100"
    />
  </view>

  <view class="total-section">
    <text class="total-label">合计金额</text>
    <text class="total-price">¥{{totalPrice}}</text>
  </view>

  <view class="submit-area safe-bottom">
    <button class="submit-btn" bindtap="submitOrder" disabled="{{submitting}}">
      {{submitting ? '提交中...' : '提交订单'}}
    </button>
  </view>
</view>
